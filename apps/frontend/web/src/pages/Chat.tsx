import { Box, Flex } from "@chakra-ui/react"

const Chat = () => {
  return (
      <Flex><Box h={'calc(100vh-80px)'} w={'30%'} position={'sticky'} top={'0'}></Box>
      <Box></Box></Flex>
  )
}

export default Chat